{{define "Title"}}Campuses{{end}}
{{define "body"}}
	{{template "header" .}}

<section class="featured">
      <div class="container"> 

    <div class="container">
      <h1>Error</h1>

      <table class="table table-striped">
        {{.Error}}

      </table>
    </div>
    </div>
</section>

	<script>
   
	</script>
	{{template "footer" .}}
{{end}}
